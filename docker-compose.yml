version: '3.8'
services:
  server:
    image: amy-server:latest
    network_mode: host
    volumes:
      - /volume2/amy_resource/archive:/amy/archive
      - /volume2/amy_resource/poster:/amy/poster
      - /volume2/amy_resource/avatar:/amy/avatar
      - /volume2/amy_resource/video:/amy/video_1
    environment:
      TZ: Asia/Shanghai
